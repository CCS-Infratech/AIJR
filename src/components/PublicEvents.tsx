"use client";

import Link from "next/link";
import { ArrowLeft, CalendarDays } from "lucide-react";
import { motion } from "framer-motion";
import Navbar from "@/components/Navbar";

type Item = { id: string; type: "EVENT" | "NEWS"; title: string; excerpt: string | null; body: string; category: string | null; eventStart: string | null; location: string | null; coverUrl: string | null; coverAlt: string };

export default function PublicEvents({ events }: { events: Item[] }) {
  const date = (value: string | null) => value ? new Intl.DateTimeFormat("en-IN", { day: "2-digit", month: "short" }).format(new Date(value)).split(" ") : null;
  return <><Navbar /><main className="overflow-hidden bg-[#f8f7f1]"><section className="relative overflow-hidden bg-[#056839] text-white"><div className="absolute inset-0 bg-[radial-gradient(circle_at_15%_20%,rgba(255,255,255,.10),transparent_28%),radial-gradient(circle_at_85%_80%,rgba(215,183,101,.14),transparent_30%)]"/><div className="container relative z-10 py-28 pt-36 sm:py-32 sm:pt-40"><motion.div initial={{opacity:0,y:25}} animate={{opacity:1,y:0}} transition={{duration:.7}} className="max-w-3xl"><p className="text-xs font-bold uppercase tracking-[.3em] text-[#ead493]">Events & News</p><h1 className="mt-5 text-5xl font-bold tracking-tight sm:text-7xl">Stay connected with what&apos;s happening.</h1><p className="mt-6 max-w-2xl text-base leading-8 text-white/70 sm:text-lg">Explore upcoming gatherings, initiatives and important moments from across the AIJR community.</p></motion.div></div></section><section><div className="container py-20 sm:py-28"><div className="mb-10 flex items-center gap-3 text-sm font-semibold text-[#66746c]"><CalendarDays size={18} className="text-[#056839]"/>Latest community moments</div>{events.length ? <div className="grid gap-7 lg:grid-cols-3">{events.map((event,index)=>{const eventDate=date(event.eventStart);return <motion.article key={event.id} initial={{opacity:0,y:30}} whileInView={{opacity:1,y:0}} viewport={{once:true}} transition={{duration:.6,delay:index*.08}} whileHover={{y:-8}} className="overflow-hidden rounded-[2rem] border border-[#e3e4dc] bg-white shadow-sm hover:shadow-xl"><div className="relative aspect-[16/10] overflow-hidden bg-[#034d2a]">{event.coverUrl?<img src={event.coverUrl} alt={event.coverAlt} className="h-full w-full object-cover"/>:<div className="h-full w-full bg-gradient-to-br from-[#056839] to-[#034d2a]"/>}{eventDate&&<div className="absolute left-5 top-5 flex h-16 w-16 flex-col items-center justify-center rounded-2xl bg-white text-[#15231c] shadow-lg"><span className="text-xl font-bold">{eventDate[0]}</span><span className="text-[9px] font-bold text-[#056839]">{eventDate[1]}</span></div>}</div><div className="p-6"><p className="text-xs font-bold uppercase tracking-[.18em] text-[#056839]">{event.category||event.type}</p><h2 className="mt-3 text-2xl font-semibold">{event.title}</h2><p className="mt-3 text-sm leading-7 text-[#66746c]">{event.excerpt||event.body}</p>{event.location&&<p className="mt-3 text-xs font-semibold text-[#66746c]">{event.location}</p>}</div></motion.article>})}</div>:<div className="rounded-[2rem] border border-[#e3e4dc] bg-white p-10 text-center"><h2 className="text-2xl font-semibold">No published events or news yet.</h2><p className="mt-3 text-sm text-[#66746c]">Please check back for upcoming community updates.</p></div>}<Link href="/" className="mt-10 inline-flex items-center gap-2 text-sm font-bold text-[#056839]"><ArrowLeft size={16}/>Back to Home</Link></div></section></main><footer className="bg-[#034d2a] text-white"><div className="container py-10 text-xs text-white/35">© {new Date().getFullYear()} All India Jamat Rayeen. All rights reserved.</div></footer></>;
}
