import { deleteMembership, updateMembership } from "@/lib/admin-content-actions";
import { prisma } from "@/lib/prisma";
export default async function MembershipAdmin({searchParams}:{searchParams:Promise<{q?:string}>}){const {q=""}=await searchParams;const rows=await prisma.membershipApplication.findMany({where:q?{OR:[{fullName:{contains:q,mode:"insensitive"}},{email:{contains:q,mode:"insensitive"}},{phone:{contains:q}}]}:undefined,orderBy:{createdAt:"desc"},take:100});return <><p className="text-xs font-bold uppercase tracking-[.28em] text-[#056839]">Applications</p><h1 className="mt-3 text-3xl font-bold">Membership</h1><form className="mt-6"><input name="q" defaultValue={q} placeholder="Search name, email or phone" className="rounded-xl border p-3 text-sm"/><button className="ml-2 rounded-xl bg-[#056839] px-4 py-3 text-sm font-bold text-white">Search</button></form><section className="mt-6 overflow-x-auto rounded-[1.5rem] border bg-white"><table className="w-full min-w-[800px] text-sm"><thead className="bg-[#f8f7f1] text-left"><tr><th className="p-4">Applicant</th><th>Father&apos;s Name</th><th>Contact</th><th>Status</th><th>Created</th><th className="p-4">Actions</th></tr></thead><tbody>{rows.map(row=><tr className="border-t" key={row.id}><td className="p-4"><b>{row.fullName}</b><p className="text-xs text-[#66746c]">{row.address||"No address"}</p></td><td>{row.fatherName}</td><td>{row.email}<br/>{row.phone}</td><td><form action={updateMembership}><input type="hidden" name="id" value={row.id}/><select name="status" defaultValue={row.status} className="rounded border p-1 text-xs"><option>NEW</option><option>REVIEWED</option><option>CONTACTED</option><option>APPROVED</option><option>REJECTED</option></select><button className="ml-1 text-xs text-[#056839]">Save</button></form></td><td>{row.createdAt.toLocaleDateString("en-IN")}</td><td className="p-4"><details><summary className="cursor-pointer text-[#056839]">View</summary><p className="mt-2 max-w-xs whitespace-pre-wrap">{row.message||"No message"}</p></details><form action={deleteMembership}><input type="hidden" name="id" value={row.id}/><button className="mt-2 text-xs text-red-700">Delete</button></form></td></tr>)}</tbody></table></section></>}
