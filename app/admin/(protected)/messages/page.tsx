import { deleteMessage, updateMessage } from "@/lib/admin-content-actions";
import { prisma } from "@/lib/prisma";
export default async function MessagesAdmin({searchParams}:{searchParams:Promise<{q?:string}>}){const {q=""}=await searchParams;const rows=await prisma.contactMessage.findMany({where:q?{OR:[{name:{contains:q,mode:"insensitive"}},{email:{contains:q,mode:"insensitive"}},{subject:{contains:q,mode:"insensitive"}}]}:undefined,orderBy:{createdAt:"desc"},take:100});return <><p className="text-xs font-bold uppercase tracking-[.28em] text-[#056839]">Inbox</p><h1 className="mt-3 text-3xl font-bold">Messages</h1><form className="mt-6"><input name="q" defaultValue={q} placeholder="Search messages" className="rounded-xl border p-3 text-sm"/><button className="ml-2 rounded-xl bg-[#056839] px-4 py-3 text-sm font-bold text-white">Search</button></form><section className="mt-6 overflow-x-auto rounded-[1.5rem] border bg-white"><table className="w-full min-w-[800px] text-sm"><thead className="bg-[#f8f7f1] text-left"><tr><th className="p-4">Sender</th><th>Subject</th><th>Status</th><th>Created</th><th className="p-4">Actions</th></tr></thead><tbody>{rows.map(row=><tr className="border-t" key={row.id}><td className="p-4"><b>{row.name}</b><p className="text-xs text-[#66746c]">{row.email}</p></td><td>{row.subject}</td><td><form action={updateMessage}><input type="hidden" name="id" value={row.id}/><select name="status" defaultValue={row.status} className="rounded border p-1 text-xs"><option>NEW</option><option>READ</option><option>REPLIED</option><option>CLOSED</option></select><button className="ml-1 text-xs text-[#056839]">Save</button></form></td><td>{row.createdAt.toLocaleDateString("en-IN")}</td><td className="p-4"><details><summary className="cursor-pointer text-[#056839]">View</summary><p className="mt-2 max-w-sm whitespace-pre-wrap">{row.message}</p></details><form action={deleteMessage}><input type="hidden" name="id" value={row.id}/><button className="mt-2 text-xs text-red-700">Delete</button></form></td></tr>)}</tbody></table></section></>}
